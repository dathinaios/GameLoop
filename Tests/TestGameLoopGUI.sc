TestGameLoopGUI : UnitTest{ var gameloop, visualiser, gameLoopGUI;

  setUp {
    /* //stub */
    /* gameloop = (); */
    /* visualiser = (); */
    /* gameloop['addDependant'] = true; */
    /* gameloop['removeDependant'] = true; */
    /* gameloop['entManager'] = true; */
    /* gameloop['repManager'] = true; */
    /* gameloop = GameLoop(20, 20, 1); */
    /* visualiser = GameLoopVisualiser(gameloop.entManager, gameloop.repManager); */
    /* /1* visualiser['mainView'] = nil; *1/ */
    /* gameLoopGUI = GameLoopGUI(gameloop, visualiser); */
  }

  tearDown {
    /* gameLoopGUI.clear; */
  }

  test_gui{
    /* this.assert(gameLoop.addDependant, "it adds itself as a dependant to the object passed in as gameloop"); */
    /* this.assert(GameLoopGUI.instance == gameLoopGUI, "it should have the instance stored in a class variable"); */
    /* this.assert(GameLoopGUI(gameloop) == GameLoopGUI , "it should return an error and the class if we try to create another instance"); */
  }

  test_clear{
    /* this.assert(gameloop.removeDependant, "it sends removeDependant to gameloop"); */
  }

}
